# Design references

The supplied HTML, CSS, migration notes, and full-resolution source photography are kept here for implementation reference. Only optimized, explicitly used images belong in `public/`, so Next.js does not deploy the full reference library.

`source-images/` is intentionally ignored by Git. Keep the local originals when producing new crops or formats; do not link to them from application code.
